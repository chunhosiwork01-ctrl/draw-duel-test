services:
  - type: web
    name: draw-duel-test
    runtime: python
    plan: free
    rootDir: draw_duel
    buildCommand: python3 -m py_compile server.py
    startCommand: python3 server.py
    healthCheckPath: /healthz
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.8
